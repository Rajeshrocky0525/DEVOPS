Basic Linux Commands
1. ls -l-----> long list along with files, directories and permissions. Shows in Alpabet order (A to Z)
2. ls -lr----->.long list along with files, directories and permission but in reverse Alphabet order (Z to A)
